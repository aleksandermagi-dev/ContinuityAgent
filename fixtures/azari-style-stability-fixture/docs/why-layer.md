# Why Layer Notes

The service should remember why decisions were made, not only the final task list. Validation checks are stability signals, not tasks by default.

Unresolved branch: future local plugin bridge can ask for continuity packets, but writes must remain drafts until accepted.
