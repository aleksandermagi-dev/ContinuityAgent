# Old Authority Note

This note is intentionally stale. It claims the project should merge all helper agents into one identity, which conflicts with the current standalone continuity-service direction.
