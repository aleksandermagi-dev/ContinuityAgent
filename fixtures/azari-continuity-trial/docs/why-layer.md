# Why Layer Notes

The continuity helper should preserve rationale, constraints, tradeoffs, failed paths, and reversal conditions.

Azari can later use the helper through a local API bridge, but the helper must remain universal for any complex project.

Important reversal condition: if compact packets lose provenance, the integration should pause until source tracing is restored.
